import React, { ReactElement } from 'react'
import styled from 'styled-components'

const Container = styled.div`
    flex: 1 1 0px;
`

export const OverallMatchScore = (): ReactElement => {
    return <Container>Hola</Container>
}
